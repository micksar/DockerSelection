Numpy library has been successfully installed.
In order to ensure that, you can run the image and open an interactive shell with the command: "docker exec -it <image id> bash"
After that you can find the version of the numpy that it is running on the system with the command: "python3 -c "import numpy; print(numpy.__version__)""
